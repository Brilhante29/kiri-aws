services:
  kiri:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "4566:4566"
    environment:
      - KIRI_HOST=0.0.0.0
      - KIRI_PORT=4566
      - KIRI_LOG_LEVEL=debug
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:4566/health"]
      interval: 5s
      timeout: 3s
      retries: 5
