apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "kiro.fullname" . }}
  labels:
    {{- include "kiro.labels" . | nindent 4 }}
spec:
  replicas: 1
  serviceName: {{ include "kiro.fullname" . }}
  selector:
    matchLabels:
      {{- include "kiro.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "kiro.selectorLabels" . | nindent 8 }}
    spec:
      terminationGracePeriodSeconds: 35
      securityContext:
        runAsNonRoot: true
        runAsUser: 10000
        runAsGroup: 10000
        fsGroup: 10000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: kiro
          image: "{{ .Values.kiro.image.registry }}/{{ .Values.kiro.image.repository }}:{{ .Values.kiro.image.tag | default .Chart.AppVersion }}"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          ports:
            - name: http
              containerPort: 4566
              protocol: TCP
          env:
            - name: KIRO_DATA_DIR
              value: "/data"
            - name: KIRO_LOG_LEVEL
              value: {{ .Values.kiro.logLevel | quote }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            {{- toYaml .Values.kiro.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
{{- if .Values.persistence.storageClass }}
        storageClassName: {{ .Values.persistence.storageClass | quote }}
{{- end }}
        resources:
          requests:
            storage: {{ .Values.persistence.size }}
