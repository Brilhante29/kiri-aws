{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "kiri.fullname" . }}
  labels:
    {{- include "kiri.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "kiri.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - {}
  egress: []
{{- end }}
